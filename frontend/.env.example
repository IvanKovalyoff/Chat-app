# Backend origin. Leave unset for local dev (Vite proxies /auth and /users to localhost:3001).
# In production, set to the deployed backend's URL, e.g. https://chat-app-api.onrender.com
VITE_API_URL=backend-production-f5bd.up.railway.app
