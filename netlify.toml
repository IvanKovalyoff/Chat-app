[build]
  command = "pnpm --filter frontend run build"
  publish = "frontend/dist"

# Client-side routing (React Router) — let index.html handle every path
# instead of Netlify 404ing on direct loads/refreshes of e.g. /login.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
